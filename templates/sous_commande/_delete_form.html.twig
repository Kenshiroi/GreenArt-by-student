<form method="post" action="{{ path('app_sous_commande_delete', {'idVariante': sous_commande.idVariante}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_commande.idVariante) }}">
    <button class="btn">Delete</button>
</form>
