{% extends 'base.html.twig' %}

{% block title %}SousCommande{% endblock %}

{% block body %}
    <h1>SousCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quantite</th>
                <td>{{ sous_commande.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sous_commande_index') }}">back to list</a>

    <a href="{{ path('app_sous_commande_edit', {'idVariante': sous_commande.idVariante}) }}">edit</a>

    {{ include('sous_commande/_delete_form.html.twig') }}
{% endblock %}
