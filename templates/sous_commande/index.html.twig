{% extends 'base.html.twig' %}

{% block title %}SousCommande index{% endblock %}

{% block body %}
    <h1>SousCommande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sous_commande in sous_commandes %}
            <tr>
                <td>{{ sous_commande.id }}</td>
                <td>{{ sous_commande.quantite }}</td>
                <td>
                    <a href="{{ path('app_sous_commande_show', {'id': sous_commande.id}) }}">show</a>
                    <a href="{{ path('app_sous_commande_edit', {'id': sous_commande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_sous_commande_new') }}">Create new</a>
{% endblock %}
