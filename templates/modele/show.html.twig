{% extends 'base.html.twig' %}

{% block title %}Modele{% endblock %}

{% block body %}
    <h1>Modele</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modele.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ modele.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ modele.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ modele.image }}</td>
            </tr>
            <tr>
                <th>QuantiteStock</th>
                <td>{{ modele.quantiteStock }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ modele.description }}</td>
            </tr>
            <tr>
                <th>Variante</th>
                <td>{{ modele.variante }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ modele.dateCreation ? modele.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PourcentageCreateur</th>
                <td>{{ modele.pourcentageCreateur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modele_index') }}">back to list</a>

    <a href="{{ path('app_modele_edit', {'id': modele.id}) }}">edit</a>

    {{ include('modele/_delete_form.html.twig') }}
{% endblock %}
